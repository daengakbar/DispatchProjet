@model IEnumerable<AceAdmin.Web.Models.JobsModel>
@{
    ViewBag.Title = "JobsList";
}
<div class="row">
    <div class="col-xs-12">
        <!-- div.dataTables_borderWrap -->
        <div>
            <table class="table table-striped table-bordered table-hover table-checkable order-column" id="ListTable">
                <thead>
                    <tr>
                        <th>Job ID</th>
                        <th>Job Number</th>
                        <th>Work ID</th>
                        <th>Vehicle ID</th>
                        <th class="text-center" width="100">#</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.nJobID)</td>
                            <td>@Html.DisplayFor(modelItem => item.cJobNum)</td>
                            <td>@Html.DisplayFor(modelItem => item.nWorkID)</td>
                            <td>@Html.DisplayFor(modelItem => item.nVehiID)</td>

                            <td class="text-center">
                                <a class="green" href="#modal-table" onclick="javascript: editData('@Html.DisplayFor(modelItem => item.nJobID)')" data-toggle="modal">
                                    <i class="ace-icon fa fa-pencil"></i>
                                </a> |
                                <a href="javascript:;" onclick="javascript: delData('@Html.DisplayFor(modelItem => item.nJobID)', '@Html.DisplayFor(modelItem => item.cJobNum)')" class="tooltip-success" data-rel="tooltip" title="Delete">
                                    <span class="red"><i class="ace-icon fa fa-trash-o bigger-130"></i></span>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section PageInlineScripts {
    <script type="text/javascript">
        jQuery(function ($) {
            var oTable1 =
            $('#ListTable').dataTable();
        })
    </script>
}
